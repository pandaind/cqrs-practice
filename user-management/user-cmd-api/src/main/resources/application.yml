server:
  port: 8081

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: user
      auto-index-creation: true

  security:
    oauth2:
      resource-server:
        jwt:
          issuer-uri: http://localhost:8083
