server:
  port: 9091

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: bank
      auto-index-creation: true

  security:
    oauth2:
      resource-server:
        jwt:
          issuer-uri: http://localhost:8083
